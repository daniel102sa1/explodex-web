import{money,n,px}from"./fmt";export default function X({rows}:{rows:any[]}){return <div className="terminal-panel overflow-x-auto"><table className="min-w-[760px] w-full text-xs"><tbody>{rows.map(r=><tr key={r.id} className="border-t border-slate-900"><td className="p-3 font-black">{r.symbol}</td><td>{r.side}</td><td>{px(r.entry_price)} → {px(r.exit_price)}</td><td className={n(r.net_pnl)>=0?"text-emerald-300":"text-rose-300"}>{money(r.net_pnl)}</td><td>{String(r.exit_reason||"—").replaceAll("_"," ")}</td><td>{r.evaluation_generation||"—"}</td></tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-slate-600">Sin operaciones cerradas.</div>}</div>}
