import{money,n}from"./fmt";
export default function EquityMini({p}:{p:any[]}){const a=p?.length?p:[{equity:1000,cumulative_pnl:0}],v=a.map(x=>n(x.equity)),lo=Math.min(...v),hi=Math.max(...v),sp=Math.max(hi-lo,1),d=a.map((x,i)=>`${i?"L":"M"} ${18+i/Math.max(1,a.length-1)*564} ${18+(hi-n(x.equity))/sp*150}`).join(" "),z=a.at(-1);return <div className="terminal-panel p-4"><div className="mb-2"><div className="text-[10px] font-black uppercase text-slate-500">Curva de equity</div><div className="text-xl font-black text-white">{money(z?.equity)}</div></div><svg viewBox="0 0 600 185" className="w-full"><line x1="18" x2="582" y1="168" y2="168" stroke="rgba(71,85,105,.4)"/><path d={d} fill="none" stroke="currentColor" className={n(z?.cumulative_pnl)>=0?"text-emerald-400":"text-rose-400"} strokeWidth="3"/></svg><div className="flex justify-between text-[10px] text-slate-600"><span>Inicio {money(a[0]?.equity)}</span><span>{Math.max(0,a.length-1)} trades</span><span>PnL {money(z?.cumulative_pnl)}</span></div></div>}
