import{parseReason,px}from"./fmt";export default function X({rows}:{rows:any[]}){return <div className="grid gap-2">{rows.map(s=>{const r=parseReason(s.reason),p=parseReason(r.prediction),a=parseReason(p.technical_arsenal);return <div key={s.id} className="terminal-panel p-3 text-xs"><b>{s.symbol} · {s.direction}</b> <i className="text-cyan-300">{p.phase||s.state}</i><div className="text-slate-500">setup {s.setup_score} · risk {s.risk_score} · arsenal {a.aggregate_bias||"—"} · {s.was_executed?"EJECUTADA":"SOLO SEÑAL"}</div><div>Entrada {px(s.entry_low)}–{px(s.entry_high)} · SL {px(s.stop_loss)} · TP1 {px(s.tp1)}</div></div>})}</div>}
