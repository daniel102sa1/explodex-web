"use client";export default function X({tab,setTab,s,h,g}:{tab:string;setTab:(x:any)=>void;s:any;h:any[];g:any[]}){const a=[["summary","Resumen"],["open",`Abiertas (${s.open_positions?.length||0})`],["closed",`Cerradas (${h.length})`],["all","Todas"],["signals",`Señales (${g.length})`]];return <div className="flex gap-2 overflow-x-auto">{a.map(x=><button key={x[0]} onClick={()=>setTab(x[0])} className={`rounded-xl border px-3 py-2 text-xs font-black ${tab===x[0]?"border-cyan-400/30 bg-cyan-400/10 text-cyan-200":"border-slate-800 text-slate-500"}`}>{x[1]}</button>)}</div>}
